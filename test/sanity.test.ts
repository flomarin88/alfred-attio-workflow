import { describe, expect, it } from 'vitest'

/**
 * Sanity placeholder per Story 1.1. Replaced by real helpers' tests in
 * subsequent stories.
 */
describe('sanity', () => {
  it('runs the test runner', () => {
    expect(1 + 1).toBe(2)
  })
})
